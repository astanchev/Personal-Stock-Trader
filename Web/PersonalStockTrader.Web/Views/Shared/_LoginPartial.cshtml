@using PersonalStockTrader.Common
@using PersonalStockTrader.Data.Models
@using Microsoft.AspNetCore.Identity
@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager

<ul class="nav">
    @if (this.User.IsInRole(GlobalConstants.AdministratorRoleName))
    {
        <li class="nav-item">
            <a class="nav-link" asp-area="Administration" asp-controller="Dashboard" asp-action="Index">
                <i class="mdi mdi-account-multiple menu-icon"></i>
                <span class="menu-title">Manage<br/>account managers</span>
            </a>
        </li>
    }
    else if (this.User.IsInRole(GlobalConstants.AccountManagerRoleName))
    {

    }
    else
    {

    }
</ul>
